import { FunctionComponent } from "react";
import { ForceGraph2D } from "react-force-graph";

const Graph2D: typeof ForceGraph2D = (props) => {
  return <ForceGraph2D {...props} />;
};

export default Graph2D;
